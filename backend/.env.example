# Connection URI for MongoDB (local or remote/Atlas)
MONGODB_URI=mongodb://127.0.0.1:27017/hrms

# JSON Web Token Secret key used for authentication
JWT_SECRET=your_jwt_secret_key_here

# Port for the backend API server (defaults to 5000)
PORT=5000
